<UserControl x:Class="Dc.App.Views.Workspace.ConfigTabPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:Dc.App.Markup">
    <StackPanel>
        <Border Style="{StaticResource DcCard}" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="{loc:Loc Config_Server}" Style="{StaticResource DcMuted}" Margin="0,4" />
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Server}" Margin="0,4" />
                <TextBlock Grid.Row="1" Grid.Column="0" Text="{loc:Loc Config_Node}" Style="{StaticResource DcMuted}" Margin="0,4" />
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Node}" Margin="0,4" TextWrapping="Wrap" />
                <TextBlock Grid.Row="2" Grid.Column="0" Text="{loc:Loc Config_Protocol}" Style="{StaticResource DcMuted}" Margin="0,4" />
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding ProtocolLabel}" Margin="0,4" />
                <TextBlock Grid.Row="3" Grid.Column="0" Text="{loc:Loc Config_TcpAddress}" Style="{StaticResource DcMuted}" Margin="0,4" />
                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding TcpAddress}" Margin="0,4" />
                <TextBlock Grid.Row="4" Grid.Column="0" Text="{loc:Loc Config_Interval}" Style="{StaticResource DcMuted}" Margin="0,4" />
                <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding Interval}" Margin="0,4" />
                <TextBlock Grid.Row="5" Grid.Column="0" Text="{loc:Loc Config_Deadband}" Style="{StaticResource DcMuted}" Margin="0,4" />
                <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding Deviation}" Margin="0,4" />
            </Grid>
        </Border>
        <Button Content="{loc:Loc Config_EditButton}"
                   Command="{Binding EditCommand}"
                   Style="{StaticResource DcBtnGhostSm}"
                   HorizontalAlignment="Left" />
    </StackPanel>
</UserControl>
