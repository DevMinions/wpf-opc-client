<ui:FluentWindow x:Class="Dc.App.Views.Shell.ShellWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:tb="http://www.hardcodet.net/taskbar"
                 Title="Dc · OPC 数据采集"
                 Icon="pack://application:,,,/Assets/dc.ico"
                 Width="1280" Height="800"
                 MinWidth="980" MinHeight="640"
                 WindowBackdropType="Mica"
                 ExtendsContentIntoTitleBar="True"
                 WindowCornerPreference="Round"
                 WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="28" />
        </Grid.RowDefinitions>

        <!-- Tray icon 必须在可视化树内才会被 WPF 实例化并 attach 到 Shell。
             之前放 Resources 里是惰性资源，从未被引用，导致图标根本没注册到系统托盘。
             wpfui 3.0.5 没有 NotifyIcon 控件，留用 H.NotifyIcon.Wpf。Lepoco 后续版本 ship 后再迁。-->
        <tb:TaskbarIcon x:Name="TrayIcon"
                        IconSource="pack://application:,,,/Assets/dc.ico"
                        ToolTipText="Dc · OPC 数据采集"
                        Visibility="Visible"
                        NoLeftClickDelay="True"
                        TrayLeftMouseUp="OnTrayShow"
                        TrayMouseDoubleClick="OnTrayShow">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="显示主窗口" Click="OnTrayShow" />
                    <MenuItem Header="切换主题"   Click="OnTrayToggleTheme" />
                    <MenuItem Header="关于…"     Click="OnTrayAbout" />
                    <Separator />
                    <MenuItem Header="退出"       Click="OnTrayExit" />
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>

        <ui:TitleBar Grid.Row="0" ShowMaximize="True"
                     Title="{Binding CurrentTitle, Mode=OneWay, StringFormat='Dc · OPC 数采  ›  {0}'}" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ui:NavigationView Grid.Column="0"
                               x:Name="RootNav"
                               PaneDisplayMode="Left"
                               OpenPaneLength="240"
                               IsBackButtonVisible="Collapsed"
                               IsPaneToggleVisible="False"
                               Width="240"
                               ItemInvoked="OnNavigationItemInvoked">
                <ui:NavigationView.FooterMenuItems>
                    <ui:NavigationViewItem Content="关于"
                                           Tag="about"
                                           Icon="{ui:SymbolIcon Info24}" />
                </ui:NavigationView.FooterMenuItems>
            </ui:NavigationView>

            <ContentControl Grid.Column="1"
                            Content="{Binding CurrentContent}"
                            Margin="8,0,0,0" />
        </Grid>

        <Border Grid.Row="2" Padding="14,0"
                Background="{DynamicResource TitleBarBackgroundBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,0">
            <DockPanel>
                <!-- 右侧：健康指示（对齐原型 ● 全部正常） -->
                <TextBlock DockPanel.Dock="Right" FontSize="11" VerticalAlignment="Center"
                           Text="{Binding HealthText, Mode=OneWay}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{DynamicResource SystemFillColorSuccessBrush}" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HealthOk}" Value="False">
                                    <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCautionBrush}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <TextBlock DockPanel.Dock="Right" Margin="0,0,14,0"
                           Text="Dc · v1.0.0"
                           Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                           FontSize="11" VerticalAlignment="Center" />
                <!-- 左侧：主题 + DB 路径 -->
                <TextBlock Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                           FontSize="11" VerticalAlignment="Center">
                    <Run Text="主题 · " /><Run Text="{Binding CurrentThemeLabel, Mode=OneWay}" />
                    <Run Text="    ·    DB · " /><Run Text="{Binding DbPath, Mode=OneWay}" />
                </TextBlock>
            </DockPanel>
        </Border>

        <!-- 非模态 toast 浮层：跨全部行、底部居中，浮在最上层。
             ISnackbarService.SetSnackbarPresenter(RootSnackbarPresenter) 在 App 启动 Show 前接线（任务 4）。-->
        <ui:SnackbarPresenter x:Name="RootSnackbarPresenter" Grid.RowSpan="3"
                              VerticalAlignment="Bottom" Margin="0,0,0,40" />
    </Grid>
</ui:FluentWindow>
